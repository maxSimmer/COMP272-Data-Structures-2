public class replaceMinimum {
    /**  Given an array A (containing a set of integers) and integer k, iteratively (a total of
k times) replace the smallest element in the set (m) by -m. Return the sum of all
the elements after the k modifications.
 Input: [-2,4,3,-5,7], 3
 Output: 17
 **/ 

 // create pq, loop through array and add array elements into pq
 // if or while pq is NOT empty
 // loop through pq length K times
 // find lowest value (poll) each time and multiply by -m (k times)
 // add updated value back to pq
 // for loop through the pq to add sum
 // return sum

 // create pq, loop through array list and add elements into pq
 // loop through pq, use poll to remove lowest values and add together
 // increase counter for every value polled and added.

 // if total > 100,



 

 
}
